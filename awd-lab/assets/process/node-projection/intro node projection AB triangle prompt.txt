To simplify the projection, I only want the node to attach to the top of the surface. I have attached some mock-ups to show where points A and B are, which are the two points on each of the edges of the top of the surface. So the node, wherever it's positioned, will project a triangle from the center of the node and the first two points of the top of the surface. So you see, there's one node at the top, one node positioned at the bottom. They still both have... I guess point C would be the point that touches the middle of the node. So you can see how that relationship would work. I'm thinking this would be simpler than trying to attach multiple triangles to multiple surfaces. And then you can display the projection from the top down. So from points A and B, then the surface content that is called by each node can just do a transition wipe from the top to the bottom and then display. And right now, I would just do like a static image or something that would display, just like the last mock-up.So, let me know if this is totally different than what you are describing in this phase, as far as the interpolation that you're describing. So, as far as like the transition style of the triangle, I just wanted it to be a directional display from the center of the node to the top of the surface. So the triangle would start and then project onto the top of the surface. And then, once points A and B are attached, then points A and B would then do a reverse animation to go back into the center of the node, and then the content of the surface would display and wipe from the top down.